<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UniBite - Admin Login</title>
    <link rel="stylesheet" href="/Styles/Login.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .admin-header {
            background-color: #2c3e50;
            color: white;
            padding: 1rem;
            text-align: center;
            margin-bottom: 2rem;
        }
        
        .admin-header h1 {
            margin: 0;
            font-size: 1.5rem;
        }
        
        .admin-header p {
            margin: 0.5rem 0 0;
            font-size: 0.9rem;
            opacity: 0.8;
        }
        
        .back-to-login {
            text-align: center;
            margin-top: 1rem;
        }
        
        .back-to-login a {
            color: #2c3e50;
            text-decoration: none;
            font-size: 0.9rem;
        }
        
        .back-to-login a:hover {
            text-decoration: underline;
        }

        .error-message {
            color: #e53935;
            font-size: 0.875rem;
            margin-top: 0.5rem;
            text-align: center;
        }
    </style>
</head>
<body>

<!-- Login Form Container -->
<div class="main-content">
    <div class="login-container">
        <h2>Restaurant Login</h2>

        <form id="loginForm">
            <div class="input-group">
                <input type="email" id="email" class="input-field" placeholder="Restaurant Email" required />
            </div>

            <div class="input-group">
                <div class="password-input-wrapper">
                    <input type="password" id="password" class="input-field" placeholder="Password" required />
                    <button type="button" id="togglePassword" class="password-toggle" aria-label="Toggle password visibility">
                        <svg class="eye-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path>
                            <circle cx="12" cy="12" r="3"></circle>
                        </svg>
                    </button>
                </div>
            </div>

            <div class="remember-me">
                <input type="checkbox" id="rememberMe" />
                <label for="rememberMe">Remember me</label>
            </div>

            <div class="button-container" style="justify-content: center;">
                <button type="submit" id="submitButton" class="button" style="width: 100%;">Login to Dashboard</button>
            </div>
        </form>
        
        <div class="back-to-login">
            <a href="/login">← Back to regular login</a>
        </div>
    </div>
</div>

<script src="/JavaScript/adminAuth.js"></script>
</body>
</html> 